- Please copy your ssh private key to `~/.ssh/`
- clone the `mosip-cloudlab` repo `git clone https://github.com/aoli-al/mosip-cloudlab`
- run the bash script 
  - `cd mosip-cloudlab`
  - `bash ./config.sh`
